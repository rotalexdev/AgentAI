<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SetVolumeTool: MODIFY_AUDIO_SETTINGS is a normal permission (declared
         here, not runtime-granted). SetBrightnessTool uses WRITE_SETTINGS,
         which is a special permission granted via Settings.System.canWrite
         and is NOT declared here (checked at runtime by PermissionEvaluator). -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:name=".AgentApp"
        android:label="AgentAI"
        android:icon="@android:drawable/sym_def_app_icon"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>